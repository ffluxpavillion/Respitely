{
  "name": "server",
  "version": "1.0.0",
  "description": "A server that fetches and sorts data from the City of Toronto's Daily Shelter Occupancy API and serves it to the client.",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "build": "cd ../client && npm install --legacy-peer-deps && npm install @babel/plugin-proposal-private-property-in-object --save-dev && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.2",
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
